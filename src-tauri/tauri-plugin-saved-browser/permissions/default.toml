"$schema" = "schemas/schema.json"

[default]
description = "No permissions. Opening, moving and reading the browser are all driven from the app's Rust core; the frontend never calls this plugin."
permissions = []
